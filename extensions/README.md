# Extensions

This directory is reserved for new code written after the original 2004 coursework.

New experiments, modernized rewrites, or additional features built on the original work
belong here — not in the `original/` directory.

The `original/` directory contains only code written during the Modeling Motion course
at Evergreen State College (2004), with minimal Python 3 syntax fixes applied.
